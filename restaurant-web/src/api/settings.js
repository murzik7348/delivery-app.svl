// Settings API placeholder – must be implemented on backend.
// We deliberately do NOT return any fake/mocked data here.
// When backend provides a real endpoint (e.g. /admin/settings),
// replace these functions with proper client.get/client.put calls.

export const fetchSystemSettings = async () => {
    throw new Error('Settings API is not implemented on the backend yet.');
};

export const updateSystemSettings = async () => {
    throw new Error('Settings API is not implemented on the backend yet.');
};
